# النشر الدائم المجاني — Wasel Egypt (لينك تجريبي ثابت)

الخطة اللي اخترتها: سيرفر سحابي مجاني + بدون دومين + مشي تقديري.
النتيجة: لينك ثابت بصيغة `https://wasel-egypt.onrender.com` يفضل شغال حتى لو جهازك مقفول.

> ملحوظة أمانة: Cloudflare Pages بيستضيف مواقع ثابتة (HTML/JS) بس،
> مايقدرش يشغّل Laravel ولا MySQL. عشان كده الباك-إند والواجهة مع بعض
> على Render (نفس السيرفر — الواجهة مبنية جوه `public/`)، وحساب Cloudflare
> هتحتاجه بعدين لو جبت دومين وربطته كـ Custom Domain.

## الخطوات (10 دقايق منك + 10-15 دقيقة بناء تلقائي)

### 1. ارفع الكود على GitHub

ملفات النشر (`Dockerfile` + `render.yaml` + البيانات المؤرشفة) جاهزة في المشروع.
من فولدر المشروع:

```powershell
git push origin main
```

### 2. طلّع مفتاح التشفير (مرة واحدة)

```powershell
php artisan key:generate --show
```

انسخ القيمة اللي تبدأ بـ `base64:` — هتحتاجها في الخطوة الجاية ( Symmetric secret
`APP_KEY`).

### 3. أنشئ الخدمة على Render

1. ادخل https://dashboard.render.com وسجّل (ممكن بحساب GitHub).
2. New → **Blueprint** → اختار ريبو `Wasel-Egypt` → Apply.
3. Render هيقرأ `render.yaml` وينشئ خدمة `wasel-egypt` (خطة Free).
4. لما يطلب `APP_KEY`: الصق القيمة من الخطوة 2.
   (سيب `AI_API_KEY` و `GROQ_API_KEY` فاضيين — المساعد شغال وضع `mock` بدون مفاتيح.)
5. Create → البناء يبدأ تلقائياً (تثبيت PHP + بناء الواجهة + استيراد بيانات
   القاهرة ~3000 محطة — بياخد 10-15 دقيقة أول مرة).

### 4. تأكد إنه شغال

- افتح `https://wasel-egypt.onrender.com/api/v1/health`
  لازم يرجع `{"status":"ok","database":true,...,"osrm":false}`.
- افتح `https://wasel-egypt.onrender.com` من الموبايل وجرّب بحث
  (مثال: ميدان التحرير → 29.9773, 31.1325).
- غيّر باسورد الأدمن الافتراضي فوراً (`admin@example.com` / `password`)
  من Render → Shell:
  `php artisan tinker` ثم حدّث باسورد المستخدم.

## حدود الخطة المجانية (لازم تعرفها قبل ما تبعت اللينك)

- السيرفر **بينام بعد ~15 دقيقة خمول**، وأول طلب بعده بياخد ~دقيقة لحد ما يصحى.
- بيانات المستخدمين (حسابات، رحلات محفوظة) على SQLite مؤقت — **بتتمسح مع كل
  إعادة نشر/إعادة تشغيل**. بيانات المواصلات نفسها ثابتة (مبنية في الصورة).
- المشي محسوب **تقديري** (×1.3) والواجهة بتوضح كده — الدقة الكاملة محتاجة
  سيرفر OSRM منفصل (خطة مدفوعة).
- بلاطات الخريطة OSM المجانية مناسبة لحجم التجربة فقط.
- المشروع غير تجاري (بيانات TfC برخصة CC-BY-NC-SA) — اللينك للتجربة فقط.

## لو حبيت تطوّر بعدين

- دومين خاص: اشترِ دومين → اربطه في Render (Custom Domain) → وجّه DNS من
  Cloudflare. ساعتها حساب Cloudflare بتاعك يدخل الصورة (DNS + حماية).
- منع النوم + قاعدة بيانات دائمة: خطة Starter (~7$/شهر) + Postgres.
- دقة المشي الكاملة: خدمة OSRM على Fly.io أو VPS مع `OSRM_ENABLED=true`.
